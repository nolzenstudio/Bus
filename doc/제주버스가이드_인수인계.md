# 제주버스가이드 — 프로젝트 인수인계 문서

작성일: 2026-07-22 · 최종 수정: 2026-07-22 (배포 및 여정 선택 흐름 반영)

> 📌 이 문서 초판 이후 **GitHub Pages 배포 완료** 및 **출발→버스→도착→전광판 여정 선택 흐름**으로 크게 확장되었습니다. 최신 기능 요약은 **12절**을, 배포 결과는 **10절**을 참고하세요.

## 1. 프로젝트 개요

**목표:** 사용자의 현재 위치(GPS)를 기준으로 가장 가까운 버스 정류장을 추천하고, "이 정류장에서 특정 버스를 타고 어디까지 갈 수 있는지"와 그 버스의 실시간 도착예정시간을 보여주는 모바일 반응형 웹앱.

**핵심 흐름 (여정 선택, 2단 중첩 아코디언):**

1. 브라우저에서 GPS 위치 권한 요청 → 내장 정류장 데이터(4,079곳)에서 가장 가까운 8곳 즉시 계산
2. **정류장 선택** → 그 카드 바로 아래로 '지나는 버스' 목록 확장 (재클릭 시 접힘)
3. **버스(노선) 선택** → 그 아래로 '도착 정류장' 목록 확장 — 갈 수 있는 정류장을 거리순 표시 (재클릭 시 접힘)
4. **도착 정류장 선택** → 실시간 도착 전광판에 출발→도착 요약 + 그 버스의 도착예정정보만 필터링해 LED 스타일로 표시, 20초마다 자동 갱신

**기술 스택:** 순수 HTML/CSS/JS (프레임워크 없음, 단일 파일), Cloudflare Workers (CORS 프록시)

---

## 2. 아키텍처

```text
[사용자 브라우저]
      │  GPS 좌표 확인 + 내장 정류장 데이터로 최단거리 계산 (API 호출 없음)
      │
      │  정류장 선택 시 fetch()
      ▼
[Cloudflare Workers 프록시]  jeju-bus-proxy.nolzenstudio.workers.dev
      │  (서버 간 통신이라 CORS 제약 없음)
      ▼
[bus.jeju.go.kr]  실시간 도착예정정보 등 제공
```

### 왜 프록시가 필요한가

`bus.jeju.go.kr`의 API는 자체 홈페이지 화면에서만 호출하도록 만들어져 있어, 다른 도메인(우리 웹앱)에서 브라우저로 직접 `fetch()`하면 **CORS 정책에 의해 차단**됩니다 (`Access-Control-Allow-Origin` 헤더 없음, 실측 확인됨). 이를 우회하기 위해 Cloudflare Workers를 중간 서버로 두어, 서버-서버 통신으로 대신 요청하고 결과에 CORS 허용 헤더를 붙여 돌려줍니다.

---

## 3. 산출물 파일 목록

**현재 저장소(nolzenstudio/Bus)에 실제로 있는 파일:**

- **`index.html`** — GitHub Pages 배포 진입점(`jeju-bus-guide-final.html`의 복사본). ✅ 실제 서비스 파일
- **`jeju-bus-guide-final.html`** — 최종 완성본(마스터). 전체 정류장 데이터 내장 + 여정 선택 흐름. ✅ 편집 기준 파일
- `doc/제주버스가이드_인수인계.md` — 본 인수인계 문서
- `doc/제주특별자치도_버스정류소현황_20221101.csv` — 원본 정류장 데이터(사용자 제공). `final.html`에 내장됨
- `README.md`, `LICENSE` — 저장소 기본 파일

> ⚠️ **중요 — 두 파일 동기화:** `index.html`과 `jeju-bus-guide-final.html`은 **내용이 동일해야** 합니다. 코드는 `index.html`을 편집한 뒤 `cp index.html jeju-bus-guide-final.html`로 마스터에 반영하고 **둘 다 커밋**하는 관례로 관리됩니다. (초판 문서의 목업/테스트/중간버전 HTML — `jeju-bus-guide.html`, `-api-test.html`, `-live.html` — 은 저장소에 포함하지 않았습니다.)

---

## 4. Cloudflare Workers 프록시

- **배포 주소:** `https://jeju-bus-proxy.nolzenstudio.workers.dev`
- **Cloudflare 계정:** `nolzenstudio@gmail.com`
- **Worker 이름:** `jeju-bus-proxy`

### 현재 배포된 코드 (worker.js)

```javascript
export default {
  async fetch(request, env) {
    if (request.method === "OPTIONS") {
      return new Response(null, { headers: corsHeaders() });
    }

    const url = new URL(request.url);
    let targetUrl;

    // 카카오 로컬 검색 (목적지 필터) — 키를 노출하지 않도록 프록시에서 헤더로 붙인다
    //  /kakao/keyword?query=카멜리아힐  -> 상호·명소 검색
    //  /kakao/address?query=아라일동    -> 주소·행정동 검색
    if (url.pathname.startsWith("/kakao/")) {
      const kind = url.pathname.slice("/kakao/".length);
      if (!["keyword", "address"].includes(kind)) {
        return new Response('{"error":"unsupported"}', { status: 404, headers: corsHeaders() });
      }
      const kakaoUrl =
        `https://dapi.kakao.com/v2/local/search/${kind}.json?size=10&query=` +
        encodeURIComponent(url.searchParams.get("query") || "");
      const kres = await fetch(kakaoUrl, {
        headers: { Authorization: "KakaoAK " + env.KAKAO_REST_KEY }
      });
      return new Response(await kres.text(), {
        status: kres.status,
        headers: { "content-type": "application/json;charset=UTF-8", ...corsHeaders() }
      });
    }

    if (url.pathname.startsWith("/jejudata/")) {
      // 예: /jejudata/station?stationName=한국병원
      //  -> open.jejudatahub.net/api/proxy/{데이터셋ID}/{appkey}?stationName=한국병원
      const datasetId = "DD11ab6a6t11D16baaa1a2tD26ata161";
      targetUrl = `https://open.jejudatahub.net/api/proxy/${datasetId}/${env.JEJU_DATAHUB_KEY}${url.search}`;
    } else {
      // 예: /searchArrivalInfoList.do?station_id=405000155
      //  -> bus.jeju.go.kr/api/searchArrivalInfoList.do?station_id=405000155
      targetUrl = "https://bus.jeju.go.kr/api" + url.pathname + url.search;
    }

    const upstream = await fetch(targetUrl, {
      method: "GET",
      headers: { "Referer": "https://bus.jeju.go.kr/" }
    });

    const body = await upstream.text();

    return new Response(body, {
      status: upstream.status,
      headers: {
        "content-type": "application/json;charset=UTF-8",
        ...corsHeaders()
      }
    });
  }
};

function corsHeaders() {
  return {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, OPTIONS",
    "Access-Control-Allow-Headers": "*"
  };
}
```

### 환경 변수 (Secrets)

Worker의 **Settings → Variables and Secrets**에 등록됨:

- `JEJU_DATAHUB_KEY` — 제주데이터허브(jejudatahub.net) appkey. 현재 최종본(`-final.html`)에서는 미사용(정류장 좌표를 CSV 내장 방식으로 대체). 추후 정류장 데이터 최신화 시 재사용 가능.
- `KAKAO_REST_KEY` — 카카오 로컬 API REST 키. **목적지 필터의 장소 검색에 필요** (아래 설정 절차 참고).

### 카카오 장소 검색 설정 (목적지 필터)

1. [카카오 디벨로퍼스](https://developers.kakao.com) → 내 애플리케이션 → 애플리케이션 추가
2. **앱 키 → REST API 키** 복사 (JavaScript 키 아님)
3. Cloudflare Workers → `jeju-bus-proxy` → **Settings → Variables and Secrets** →
   `KAKAO_REST_KEY` 이름으로 Secret 등록
4. 위 worker.js의 `/kakao/keyword` 분기를 반영해 **Deploy**
5. 확인: `https://jeju-bus-proxy.nolzenstudio.workers.dev/kakao/keyword?query=성산일출봉`
   → `{"documents":[...]}` 가 나오면 정상

> 설정 전에도 앱은 동작합니다 — 이 엔드포인트 호출이 실패하면 **내장 정류소 4,079곳 이름 검색**으로 자동 대체되고,
> 팝업 하단에 "정류장 이름으로만 찾고 있어요" 안내가 표시됩니다.
> 카카오 무료 쿼터는 로컬 API 기준 일 100,000회로, 개인 사용 규모에서는 넉넉합니다.

### 호출 방식

프록시 주소 뒤에 `bus.jeju.go.kr/api/` 이하 경로를 그대로 붙이면 됩니다. 예:

```text
https://jeju-bus-proxy.nolzenstudio.workers.dev/searchArrivalInfoList.do?station_id=405002676
```

---

## 5. 사용 중인 API 명세

모두 `bus.jeju.go.kr`이 원출처이며, 위 프록시를 경유해 호출합니다. 별도 인증키 불필요 (제주특별자치도 자체 공개 API).

### 5-1. 노선 목록 — `searchBusRouteList.do`

전체 버스 노선을 한 번에 반환 (파라미터 없음, 화면에서 필터링하는 방식으로 추정).

```json
{
  "ROUTE_ID": 405410111,
  "ROUTE_NUM": "101",
  "ROUTE_NM": "101-11",
  "ROUTE_SUB_NM": "남원환승정류장(남원읍사무소)",
  "ORGT_NM": "제주국제공항2(일주동로, 516도로)",
  "ORGT_STATION_ID": 405002101,
  "DST_NM": "남원환승정류장(남원읍사무소)",
  "DST_STATION_ID": 406001604,
  "MIN_INTERVAL": 0,
  "MAX_INTERVAL": 0
}
```

### 5-2. 정류장 통과 노선 — `searchRouteByStationList.do?station_id={ID}` ⭐ 핵심

특정 정류장을 지나가는 노선 목록. **여정 선택 2단계('지나는 버스' 목록)에서 사용 중.** 각 항목의 `ROUTE_ID`는 **방향까지 구분된** 값이라, 같은 번호라도 상·하행이 서로 다른 `ROUTE_ID`로 나옵니다(예: 900-1 `405090001`→조천리, 900-2 `405090002`→반대). 앱은 이를 `ROUTE_NUM + DST_NM`(번호+방향)으로 묶어 리스트에 표시합니다.

```json
{
  "STATION_ID": 405002676,
  "STATION_NM": "김녕농협하나로마트",
  "ROUTE_ID": 405090001,
  "ROUTE_NUM": "900",
  "ROUTE_NM": "900-1",
  "ORGT_NM": "김녕장례문화센터",
  "DST_NM": "조천리",
  "DST_STATION_ID": 405000234
}
```

### 5-3. 정류장 도착예정정보 — `searchArrivalInfoList.do?station_id={ID}` ⭐ 핵심

현재 앱에서 실시간 도착시간 표시에 사용 중인 API.

```json
{
  "ARRV_STATION_ID": 405000974,
  "ROUTE_ID": 405320111,
  "ROUTE_NUM": "201",
  "ROUTE_SUB_NM": "서귀포버스터미널",
  "PLATE_NO": "제주79아3226",
  "VH_ID": 7983226,
  "PREDICT_TRAV_TM": 3,
  "REMAIN_STATION": 2,
  "CURR_STATION_NM": "평대리동동",
  "CURR_STATION_ID": 405001315,
  "LOW_PLATE_TP": "N"
}
```

- `PREDICT_TRAV_TM`: 도착까지 남은 시간(분)
- `REMAIN_STATION`: 남은 정류장 수
- **버스가 없거나 배차 간격이 길면 빈 배열 `[]`을 반환함** (정상 동작, 앱에서 "지금은 접근 중인 버스가 없어요"로 처리 중)

### 5-4. 노선경유정류장 조회 — `searchBusRouteStationList.do?route_id={ID}` ⭐ 핵심

한 노선(계통)이 순서대로 지나가는 정류장 목록. **여정 선택 3단계('도착 정류장' 목록)에서 사용 중.** 선택한 노선 그룹의 모든 계통 `ROUTE_ID`에 대해 병렬 호출한 뒤, 출발 정류장의 `STATION_ORD` 이후 정류장만 모아 `ROUTE_DIST` 차이로 거리를 계산하고 병합해 거리순으로 보여줍니다.

```json
{
  "ROUTE_ID": 405090001,
  "ROUTE_NUM": "900",
  "STATION_ORD": 2,
  "STATION_ID": 405002676,
  "STATION_NM": "김녕농협하나로마트",
  "ROUTE_DIST": 888
}
```

- `STATION_ORD`: 기점부터 몇 번째 정류장인지
- `ROUTE_DIST`: 기점부터의 누적 거리(m)

### 5-5. 전체 운행버스 위치 — `searchBusAllocList.do`

현재 운행 중인 모든 버스의 실시간 GPS 좌표. 파라미터 없이 전체 반환.

```json
{
  "VH_ID": 7983064,
  "PLATE_NO": "제주79아3064",
  "ROUTE_ID": 405410111,
  "ROUTE_NUM": "101",
  "LOCAL_X": 126.861152,
  "LOCAL_Y": 33.518313,
  "CURR_STATION_ID": 405000974,
  "CURR_STATION_NM": "세화환승정류장(세화리)",
  "LOW_PLATE_TP": "N"
}
```

- `LOCAL_X` = 경도, `LOCAL_Y` = 위도
- 초기 버전(`jeju-bus-guide-live.html`)에서는 이 데이터로 "현재 운행 중인 정류장" 목록을 만들었으나, 최종본에서는 CSV 내장 방식으로 대체됨. **지도에 버스 아이콘을 실시간으로 표시하는 기능을 추가할 때 재사용 가능**.

### 5-6. (미사용) 제주데이터허브 정류소 검색

```text
https://jeju-bus-proxy.nolzenstudio.workers.dev/jejudata/station?stationName={정류장명}
```

정류소 이름으로 좌표를 검색하는 API (jejudatahub.net 제공). 이름 검색만 가능하고 좌표/반경 검색은 미지원 확인됨. 현재 최종본에서는 CSV 내장 방식으로 대체되어 미사용이지만, appkey는 살아있으므로 필요 시 재사용 가능.

---

## 6. 정류장 데이터 (CSV 내장)

- **원본 파일:** `제주특별자치도_버스정류소현황_20221101.csv` (사용자 제공, 공공데이터)
- **기준일자:** 2022-11-01 — **이후 신설/폐지된 정류장은 반영 안 됨** (알려진 한계점)
- **인코딩:** CP949 (EUC-KR 계열) → UTF-8로 변환 후 내장
- **원본 컬럼:** 정류소아이디, 정류소명, 경도, 위도, 위치정보(주변설명), 데이터기준일자
- **내장 형식:** `[정류소ID, 정류소명, 경도, 위도]` 튜플 배열로 압축, `jeju-bus-guide-final.html` 안에 `STATIONS` 상수로 직접 포함 (총 4,079건, 약 170KB)
- **매핑 확인됨:** 이 CSV의 `정류소아이디`가 `bus.jeju.go.kr` API들의 `station_id`/`STATION_ID`와 동일한 체계임을 실제 호출로 확인함 (예: `405002676` = 김녕농협하나로마트)

### 최단거리 계산 로직 (하버사인 공식)

```javascript
function haversine(lat1, lng1, lat2, lng2){
  const R = 6371000;
  const toRad = d => d * Math.PI / 180;
  const dLat = toRad(lat2-lat1);
  const dLng = toRad(lng2-lng1);
  const a = Math.sin(dLat/2)**2 + Math.cos(toRad(lat1))*Math.cos(toRad(lat2))*Math.sin(dLng/2)**2;
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
}
```

GPS 좌표를 받으면 4,079개 정류장 전체와의 거리를 즉시 계산(API 호출 없음) → 오름차순 정렬 → 상위 8곳 표시.

---

## 7. 디자인 시스템

### 컨셉

제주의 **현무암(basalt)**, **감귤(tangerine)**, **바다(ocean)**를 팔레트 기반으로 삼고, 시그니처 요소로 실제 한국 버스정류장에 있는 **LED 도착 전광판**을 재현했습니다 (호박색 글로우, 깜빡이는 LIVE 표시등).

### 컬러 토큰

- `--basalt-900` `#15171a` — 배경 (가장 어두운 톤)
- `--basalt-800` `#1e2124` — 배경 보조
- `--basalt-700` `#292d31` — 카드 배경
- `--basalt-600` `#3a3f44` — 카드 배경 보조
- `--stone-100` `#f3efe6` — 기본 텍스트
- `--stone-300` `#cfc9ba` — 보조 텍스트
- `--tangerine-500` `#ff8a3d` — 주요 강조색 (감귤)
- `--tangerine-600` `#e86f22` — 강조색 hover
- `--ocean-500` `#159b8f` — 보조 강조색 (바다)
- `--amber-led` `#ffb300` — LED 전광판 글자색
- `--route-express` `#e6493c` — 노선 유형: 급행
- `--route-trunk` `#2f6fed` — 노선 유형: 간선
- `--route-branch` `#2fa84f` — 노선 유형: 지선
- `--route-village` `#f5a623` — 노선 유형: 마을버스

### 타이포그래피

- **본문/전체:** Pretendard Variable (CDN: `cdn.jsdelivr.net/gh/orioncactus/pretendard`)
- **LED 도착시간 숫자:** Pretendard + `font-variant-numeric: tabular-nums` + 글로우 텍스트 섀도우(`text-shadow: 0 0 10px rgba(255,179,0,.55)`)로 디지털 전광판 느낌 구현

### 레이아웃

모바일 우선 단일 컬럼(`max-width: 430px`), 상단부터: 위치 확인 바 → 가까운 정류장 리스트 → 실시간 도착 전광판. `border-radius`는 카드 14~20px로 통일.

### 시그니처 요소

**LED 도착 전광판** — 실제 버스정류장 전광판을 모사한 다크 패널(`#0c0d0f`) 위에 호박색(`--amber-led`) 숫자와 깜빡이는 LIVE 인디케이터. `PREDICT_TRAV_TM`이 1분 이하일 때 "곧 도착"으로 문구 전환.

---

## 8. 진행 경과 요약 (트러블슈팅 기록)

1. **초기 요구사항:** 위치 기반 정류장 추천 + 실시간 도착정보 → 목업 데모(`jeju-bus-guide.html`)로 UI/UX 먼저 확정
2. **실 API 조사:** `bus.jeju.go.kr`의 내부 API 엔드포인트들을 사용자가 브라우저 개발자도구(F12 Network 탭)로 직접 캡처하여 확보
3. **CORS 문제 발견:** `jeju-bus-api-test.html`로 직접 호출 테스트 → 응답 헤더에 `Access-Control-Allow-Origin` 없음 확인 → 브라우저 직접 호출 불가 판명
4. **프록시 구축:** Cloudflare Workers로 CORS 우회 프록시 배포, 여러 차례 배포 이슈 해결 (Error 1031은 미리보기 창 문제로 무관, "Apply" 버튼으로 실제 배포 적용 필요했음)
5. **API 필드 구조 확인:** 노선 목록 → 정류장 통과 노선 → 도착예정정보 → 노선경유정류장 → 전체 버스 위치 순으로 실제 호출하며 필드명 확인
6. **정류장 검색 방식 결정:** 처음엔 jejudatahub 이름 검색 시도 → 반경 검색 미지원 확인 → "노선별(현재 운행 중) 정류장"으로 임시 구현(`-live.html`) → 사용자가 공식 정류소 CSV(4,079건)를 제공하며 전체 데이터 내장 방식으로 최종 업그레이드(`-final.html`)

---

## 9. 알려진 한계 및 향후 개선 아이디어

- **정류장 데이터 최신성** — 현재: 2022-11-01 기준 / 개선: 최신 CSV로 교체 가능(동일 구조 유지 시 코드 수정 불필요)
- **목적지 이름 검색** — ✅ 구현(13절 목적지 필터). 카카오 로컬 API + 내장 정류소명 검색 병행
- **지도 시각화** — 현재: 미구현(리스트만) / 개선: `searchBusAllocList.do`(버스 위치) + 지도 라이브러리(Leaflet 등)로 실시간 버스 아이콘 표시 가능
- **노선 경로선 표시** — 현재: 미구현 / 개선: `searchBusRouteStationList.do` 좌표로 그리기 가능(좌표는 도착지 목록 계산에는 이미 활용 중)
- **배포** — ✅ 완료 (GitHub Pages, 10절 참고. Enforce HTTPS 적용됨)
- **그룹화 타임라인** — 계통 병합 방식. 계통마다 경로가 달라 '이전 정류장 회색' 대신 '도착 가능 정류장 거리순'으로 표시
- **위치 옵션** — `{timeout:8000}`만 사용. 신호 약한 곳 타임아웃 잦을 수 있음(`maximumAge`/`enableHighAccuracy`/재시도 미적용)
- **자동 새로고침 빈도** — 20초. 실사용 트래픽 보고 조정 필요

---

## 10. 배포 (GitHub Pages) — ✅ 완료

- **저장소:** `github.com/nolzenstudio/Bus` (main 브랜치)
- **서비스 주소:** <https://nolzenstudio.github.io/Bus/> (HTTPS, http 접속 시 301 자동 전환)
- **소스 설정:** Settings → Pages → Deploy from a branch, `main` / `root`
- **진입점:** `index.html` (= `jeju-bus-guide-final.html` 복사본)

### 재배포/수정 시 주의

1. 코드는 `index.html` 편집 → `cp index.html jeju-bus-guide-final.html`로 마스터 동기화 → **둘 다** 커밋 → `git push origin main`
2. 푸시하면 GitHub Pages가 1~2분 내 자동 재배포 (별도 조작 불필요)
3. **Pages 최초 활성화는 저장소 소유자 `nolzenstudio` 계정(admin)만 가능** — 협업자(push 권한)로는 API/설정으로 켤 수 없음(권장: 브라우저에서 클릭)
4. 로컬 리모트는 SSH 키 미등록으로 **HTTPS**로 연결되어 있음 (`git remote -v` → https)

---

## 11. 계정/자격 정보 메모

- **Cloudflare 계정:** `nolzenstudio@gmail.com`
- **Cloudflare Worker 이름:** `jeju-bus-proxy`
- **Worker 배포 주소:** <https://jeju-bus-proxy.nolzenstudio.workers.dev>
- **jejudatahub.net appkey:** Cloudflare Secret `JEJU_DATAHUB_KEY`로 저장됨 (코드에 노출 안 됨)

> 이 문서에는 실제 비밀값(appkey 등)을 적지 않았습니다. 필요 시 Cloudflare 대시보드 → Worker → Settings → Variables and Secrets에서 확인하세요.

---

## 12. 여정 선택 흐름 & UI 구조 (초판 이후 추가)

초판의 "정류장 선택 → 그 정류장의 모든 도착정보 표시"에서, **"이 정류장에서 특정 버스를 타고 어디까지 갈 수 있는지"**까지 안내하는 여정 선택 흐름으로 확장했습니다.

### 12-1. 4단계 흐름 (2단 중첩 아코디언)

```text
가까운 정류장(8곳)
  └─[정류장 클릭·확장]─ 지나는 버스 (번호+방향으로 그룹화)
        └─[노선 클릭·확장]─ 도착 정류장 (계통 병합, 거리순)
              └─[도착지 클릭]─ 실시간 전광판 (출발→도착 요약 + 그 버스만 필터)
```

- **확장/축소 토글:** 정류장·노선 카드를 다시 클릭하면 접힘.
- **DOM 이동 방식:** 상세 패널(`#stationDetail`, `#routeDetail`)을 선택 카드 아래 슬롯(`.station-slot`, `.detail-slot`)으로 `appendChild` 이동하고, 리스트를 다시 그리기 전엔 보관함(`#stationDetailHolder`, `#detailHolder`)으로 회수한다. 이렇게 하면 `timelineArea`/`boardArea` 등 고정 ID를 유지한 채 렌더 함수를 재사용할 수 있고, 재렌더 시 노드가 파괴되지 않는다.

### 12-2. 노선 그룹화 (핵심 로직)

제주 버스는 **한 노선번호 아래 수십 개의 세부 계통(운행 변형)**이 있어, 허브 정류장에서 같은 번호가 여러 번 나온다(예: 제주버스터미널의 `232`가 12개, `201`이 9개). 각 계통은 서로 다른 `ROUTE_ID`를 가진 별개 노선이다(완전 중복 `ROUTE_ID`는 없음).

- **리스트:** `ROUTE_NUM + DST_NM`(번호+방향)으로 묶어 1줄로 표시(예: "232 · 제주버스터미널 방면 · 9개 계통 운행").
- **도착지 목록:** 그룹의 모든 계통 `ROUTE_ID`에 `searchBusRouteStationList.do`를 병렬 호출 → 출발 정류장(`STATION_ORD`) 이후 정류장만 모아 병합. 정류장별로 최소 거리(`ROUTE_DIST` 차)와 '지나는 계통 집합'을 기록하고 거리순 정렬. 일부 계통만 지나는 정류장엔 `n/N 계통` 배지.
- **전광판 필터:** `searchArrivalInfoList.do` 결과를 **선택한 도착지를 실제로 지나는 계통 `ROUTE_ID` 집합**으로만 필터. → 같은 번호라도 그 도착지를 안 가는 계통·반대 방향은 제외하되, 먼저 오는 다른 계통은 놓치지 않음.

### 12-3. 위치 권한 UX

- **권한 거부(`err.code === 1`) 감지:** iOS(Safari)/안드로이드(Chrome) 재설정 안내 카드 표시. `navigator.permissions`로 시작 시 상태를 선확인해 이미 거부면 프롬프트 없이 안내부터 노출. `userAgent`로 OS 자동 감지.
- **제주 밖 위치:** 가장 가까운 정류장이 40km(`OUT_OF_RANGE_M`) 초과면 기준 위치를 **제주국제공항**(`AIRPORT_LOC`, 정류소 405001896 좌표)으로 대체하고 안내 배너 표시. 권한 거부/GPS 실패 시 기본값은 기존대로 제주시청(`DEFAULT_LOC`).

### 12-4. 주요 함수 맵 (index.html `<script>`)

- `initLocate` / `startLocate` — 권한 선확인 → GPS 위치 획득(실패 시 안내/기본위치)
- `loadNearbyStations` / `renderStations` — 4,079곳 하버사인 거리 → 가까운 8곳(+범위밖 배너)
- `selectStation` — 정류장 선택/토글 → `mountStationDetail` → `loadRoutes`
- `loadRoutes` / `renderRoutes` — 통과 노선 조회 → 번호+방향 그룹화 리스트
- `selectGroup` — 노선 그룹 선택/토글 → `mountDetail` → `loadGroupTimeline`
- `loadGroupTimeline` / `renderGroupTimeline` — 계통 병렬조회·병합 → 도착 가능 정류장 거리순
- `selectGroupDest` — 도착지 선택 → 계통 집합 확정 → `loadArrivals`
- `loadArrivals` / `renderBoard` — 도착예정 조회 → 계통 필터 → 전광판(20초 갱신)
- `mount*/park*Detail`, `reset*` — 상세 패널 DOM 이동·상태 초기화

### 12-5. 목적지 필터 (13절 참고)

상단 **"목적지 입력해서 버스 찾기"** 버튼 → 바텀시트 팝업. 상세는 13절.

### 12-6. 작업/검증 관례

- 편집 후 `<script>` 블록을 추출해 `node --check`로 JS 문법 검증.
- 프록시 점검은 `curl` 사용(파이썬 기본 UA는 403).
- `index.html` ↔ `jeju-bus-guide-final.html` 동기화 후 한국어 커밋 메시지로 커밋 → `git push origin main`.

---

## 13. 목적지 필터 (팝업)

"가고 싶은 곳"을 입력해 **거기까지 가는 버스**를 역방향으로 찾는 흐름. 기존 4단계 흐름(정류장 → 노선 → 도착지)의 반대 방향 진입점이다.

### 13-1. 흐름

```text
[목적지 입력해서 버스 찾기] 버튼
  └─ 검색어 입력
       ├─ 내장 정류소 4,079곳 이름 부분일치 (즉시, 오프라인)
       └─ 카카오 로컬 키워드 검색 (프록시 경유, 제주 영역만)
  └─ 장소 선택 → 그 좌표 반경 900m(PLACE_RADIUS_M) 내 정류장 후보 최대 6곳
       └─ 도착 정류장 지정
            ├─ [직통 노선] 출발 정류장 → 도착 정류장으로 실제 이어지는 노선만
            └─ [이 정류장 전체] 도착 정류장에 서는 모든 노선(직통엔 배지)
                 └─ 노선 클릭 → 팝업 닫고 메인 흐름에 반영(실시간 전광판까지 자동 진행)
```

- 반경 안에 정류장이 하나도 없으면 가장 가까운 3곳으로 대체.
- 출발 정류장은 **가까운 정류장 8곳(`nearbyList`) 칩**에서 선택. 기본값은 화면에서 이미 펼친 정류장, 없으면 최근접.

### 13-2. 직통 판정 로직

1. `searchRouteByStationList.do`를 출발·도착 정류장에 각각 호출
2. 두 결과의 **`ROUTE_ID` 교집합**만 후보로 추림 (보통 몇 개)
3. 후보 계통에만 `searchBusRouteStationList.do`를 병렬 호출 → 출발 `STATION_ORD` < 도착 `STATION_ORD`인 계통만 통과(**반대 방향 제외**)
4. 통과한 계통을 `ROUTE_NUM + DST_NM`으로 묶고, 그룹의 최단 계통 기준으로 `n개 정류장 · 거리` 표시

교집합을 먼저 구하므로 경유정류장 조회 횟수가 최소화되고, 조회 결과는 `routeStationCache`(ROUTE_ID → Promise)·`stationRouteCache`(STATION_ID → Promise)에 캐시해 재조회하지 않는다.

**직통이 0건일 때 — 대안 조합 탐색(`findAlternatives`)**

제주는 **방면별로 승차장이 갈라진** 곳이 많다. 대표적으로 제주국제공항은 `제주국제공항`(405001896, 주로 도착·시내방면), `제주국제공항1(표선,성산,남원)`(405001897), `제주국제공항2(일주동로,516도로)` 등으로 나뉘어, 최근접 정류장만 보면 "성산 가는 버스 없음"이 나온다(실측 확인).

그래서 직통 결과가 비면 **가까운 정류장 8곳 × 목적지 주변 후보 정류장**의 모든 조합을 확인해, 갈 수 있는 조합을 최대 5개까지 제안한다(출발 거리 → 도착 거리 순). 항목을 누르면 `useAlt(depId, destId)`가 출발·도착을 함께 바꿔 재조회한다.

- 실측 예: 제주국제공항 → 성산일출봉입구 = 직통 없음 → 제안 `제주국제공항1(표선,성산,남원) → 성산일출봉입구 (111, 111-1)`, `제주국제공항2(일주동로,516도로) → 성산일출봉입구 (101)`

### 13-3. 장소 검색 이중화

- **카카오 로컬 API** — `/kakao/keyword`(상호·명소)와 `/kakao/address`(주소·행정동)를 **동시에 호출해 병합**한다(프록시가 REST 키를 헤더로 붙임). 키워드 검색은 장소 색인이라 "인화동" 같은 동 이름이 0건이 될 수 있어 주소 검색을 함께 쓴다. 응답은 `JEJU_BOUNDS`(위도 32.95~33.65, 경도 125.95~127.05)로 도외 결과를 걸러내고, 이름+좌표(소수 4자리)가 같으면 중복 제거한다. 한쪽만 실패하면 나머지 결과로 계속 진행하고, **둘 다 실패해야** `kakaoDown` 처리.
- **내장 정류소명 검색** — 항상 함께 실행되며 목록 위쪽에 배치. 카카오 호출이 실패하면(키 미설정·쿼터 초과·장애) `filter.kakaoDown` 플래그가 서고 이쪽 결과만으로 계속 동작한다.

### 13-4. 주요 함수 (index.html `<script>`)

- `openFilter` / `closeFilter` — 바텀시트 열기·닫기(딤 영역 클릭 시 닫힘)
- `runPlaceSearch` — 정류소명 검색 + 카카오 검색 병행 → `filter.places`
- `pickPlace` — 장소 좌표 기준 반경 내 정류장 후보(`filter.cands`)
- `pickDest` / `pickDep` — 도착·출발 정류장 지정(변경 시 조회 결과 폐기)
- `findDirectRoutes` / `findDirectBetween` — 13-2 직통 판정
- `findAlternatives` / `useAlt` — 직통 0건일 때 출발·도착 조합 대안 탐색·적용
- `groupRoutes` — 번호+방면 그룹화(메인 화면 규칙과 동일)
- `applyPlan` — 팝업을 닫고 `selectStation → selectGroup → selectGroupDest` 순으로 메인 흐름 재생
- `renderFilterStep` / `renderPlaces` / `renderCands` / `renderPlanTabs` — 단계별 렌더링

### 13-5. 검증

`node vm`으로 `<script>`를 실행하고 fetch를 목으로 대체해 흐름 전체를 검증했다(장소검색 폴백, 반경 후보, 직통/역방향/미경유 판정, 계통 병합, 캐시 재사용, `applyPlan` 반영까지 23항목). 카카오 키 설정 전에는 실기기에서 "정류장 이름으로만 찾고 있어요" 안내가 뜨는 것이 정상.
